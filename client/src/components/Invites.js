import React from 'react';
import PropTypes from 'prop-types';

const testInvites = [];

const Invites = (props) => {
  return (
    <div>
      <h1>Invites</h1>
    </div>
  );
};

Invites.propTypes = {};

export default Invites;
