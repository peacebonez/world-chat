import React from 'react';
import PropTypes from 'prop-types';

const ChatList = (props) => {
  return (
    <div>
      <h1>Chats</h1>
    </div>
  );
};

ChatList.propTypes = {};

export default ChatList;
